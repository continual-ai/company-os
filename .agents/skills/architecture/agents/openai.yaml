interface:
  display_name: "Architecture"
  short_description: "Product and ownership decision context"
  default_prompt: "Use $architecture for non-obvious product and ownership context, inspect the code, and keep current choices open to challenge."
