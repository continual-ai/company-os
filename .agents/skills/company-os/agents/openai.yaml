interface:
  display_name: "Company OS"
  short_description: "Evolving Company OS decision context"
  default_prompt: "Use $company-os for non-obvious product and ownership context, inspect the code, and keep current choices open to challenge."
