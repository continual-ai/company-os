{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["VITE_PORTAL_URL", "VITE_WORKSPACE_URL"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".output/**", "dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "dev": {
      "cache": false,
      "env": ["VITE_PORTAL_URL", "VITE_WORKSPACE_URL"],
      "persistent": true
    }
  }
}
