import { defineInterface } from "@continual/runtime"

export const Identity = defineInterface({
  id: "identity",
  name: "Identity",
  pluralName: "Identities",
  description:
    "A human or non-human identity that may act in Acme's Company OS.",
})
