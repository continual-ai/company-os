# @acme/model

The complete public Acme Company Model. It defines Acme's Objects,
relationships, Tools, Modules, and Apps with `@continual/model`.

This package contains no Tool handlers, database access, authorization
decisions, server framework code, or secrets. Those private implementation
concerns are composed in `@acme/api`.
