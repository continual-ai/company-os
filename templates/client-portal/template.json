{
  "id": "client-portal",
  "destination": "client-portal",
  "package": {
    "name": "client-portal",
    "scripts": {
      "build": "vite build",
      "dev": "vite dev --port 3001",
      "preview": "vite preview --port 3101",
      "typecheck": "tsc --noEmit"
    }
  },
  "replacements": [
    {
      "files": ["README.md"],
      "from": "@company-template/client-portal",
      "to": "client-portal"
    },
    {
      "files": ["README.md"],
      "from": "localhost:3201",
      "to": "localhost:3001"
    }
  ],
  "bootstrapCommands": [["pnpm", "--filter", "client-portal", "typecheck"]]
}
