{
  "name": "Company OS",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "user": "ubuntu",
  "install": "pnpm install --frozen-lockfile",
  "start": "bash .cursor/start.sh",
  "ports": [
    {
      "name": "company-os",
      "port": 3002
    }
  ],
  "terminals": [
    {
      "name": "company-os",
      "command": "pnpm dev",
      "description": "Company OS at http://localhost:3002 after docker compose up -d (Postgres on localhost:5433)"
    }
  ]
}
